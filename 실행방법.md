# AI 면접 시스템 — 다른 컴퓨터에서 실행하기

## 1. 파일 옮기기

**폴더 전체**를 통째로 옮겨야 합니다. `index.html` 하나만 옮기면 영상·음성이 모두 빠집니다.

```
AI_Interview_s2/
├── index.html          ← 프로그램 본체
├── start_windows.bat   ← Windows 실행 파일
├── server.ps1          ← Windows 내장 서버 (bat 이 자동 실행)
├── start.command       ← Mac 실행 파일
└── assets/             ← 영상·음성·이미지 (필수)
```

`start_windows.bat` 과 `server.ps1` 은 **같은 폴더에 함께** 있어야 합니다.

용량이 약 **320MB**라 메일 첨부는 어렵습니다. 아래 중 하나를 쓰세요.

- **USB 메모리** — 가장 확실하고 빠름
- **구글 드라이브 / 드롭박스** — 폴더를 압축(.zip)해서 업로드 → 상대가 받아서 압축 해제
- 압축 방법(Mac): 폴더 우클릭 → "AI_Interview_s2 압축"

## 2. 실행하기 — Windows

`start_windows.bat` **더블클릭**. 그게 전부입니다.

검은 창이 뜨고 브라우저가 자동으로 열립니다. 주소는 `http://localhost:8000` 입니다.
검은 창은 **면접이 끝날 때까지 닫지 마세요** (닫으면 프로그램이 중단됩니다).

**설치할 것은 없습니다.** Python이 없으면 윈도우에 기본 내장된 PowerShell로 자동 전환됩니다.

### 파란색 경고창("Windows의 PC 보호")이 뜰 때

인터넷에서 받은 파일이라 뜨는 경고입니다.
**추가 정보** → **실행** 을 누르면 됩니다.

### 방화벽 창이 뜰 때

**액세스 허용**을 누르세요. 내 컴퓨터 안에서만 쓰는 서버라 외부와 통신하지 않습니다.

### 압축 해제 시 주의

`.zip`을 **더블클릭해서 바로 실행하지 마세요.** 윈도우는 압축 파일 안을 미리보기로 열어주는데,
그 상태로 실행하면 영상 파일을 못 찾습니다.
`.zip` **우클릭 → 압축 풀기(모두 추출)** 로 폴더를 꺼낸 뒤 실행하세요.

---

## 2-B. 실행하기 — Mac

`start.command` 더블클릭.

### Mac에서 "확인되지 않은 개발자" 경고가 뜰 때

`start.command`를 **우클릭 → 열기 → 열기**를 누르면 실행됩니다. 처음 한 번만 하면 됩니다.

### Mac에서 더블클릭해도 아무 반응이 없거나 텍스트 편집기로 열릴 때

구글 드라이브·메신저를 거치면서 실행 권한이 사라진 경우입니다. 터미널을 열고
`chmod +x ` 를 입력한 뒤 (뒤에 **공백** 하나) `start.command` 파일을 터미널 창으로
**드래그**해서 경로를 채우고 엔터를 누르세요. 처음 한 번만 하면 됩니다.

```
chmod +x /Users/사용자명/Desktop/AI_Interview_s2/start.command
```

### Mac에서 "명령어 라인 개발자 도구" 설치 창이 뜰 때

Python이 없는 macOS입니다. 창에서 **설치**를 누르고 (5~10분) 끝난 뒤 다시 더블클릭하세요.
또는 터미널에 `xcode-select --install` 을 붙여넣어도 됩니다.

## 3. 꼭 알아둘 것

**반드시 `localhost` 로 열어야 합니다.** `index.html`을 그냥 더블클릭하면(주소가 `file:///…`)
브라우저 보안 정책 때문에 **카메라와 마이크가 작동하지 않습니다.**

**Chrome 사용을 권장합니다.** 카메라·마이크 권한 요청이 뜨면 **허용**을 눌러 주세요.
잘못 눌러 차단했다면 주소창 왼쪽 자물쇠 아이콘 → 카메라/마이크를 "허용"으로 바꾸고 새로고침하세요.

## 4. 별도로 설치할 것

**없습니다.** 인터넷 연결도 필요 없습니다.
Windows는 내장 PowerShell로, Mac은 내장 Python으로 자동 실행됩니다.

## 5. 실험 데이터 받기

면접이 끝나면 마지막 화면에서 실험자용 버튼 두 개가 있습니다.

- **로그 CSV 저장** — 조건, 문항별 시각, 다시듣기 여부, 답변 사용 시간
- **답변 녹음 전체 저장** — 문항별 음성 파일

파일은 브라우저의 기본 다운로드 폴더에 저장됩니다.
**브라우저를 닫으면 사라지므로 반드시 종료 화면에서 저장하세요.**

## 6. 설문 링크 연결 (선택)

`index.html`을 텍스트 편집기로 열어 위쪽의 다음 줄을 찾습니다.

```js
const SURVEY_URL = "";
```

따옴표 안에 설문 주소를 넣으면 마지막 화면의 버튼이 설문으로 연결됩니다.

```js
const SURVEY_URL = "https://forms.gle/xxxxx";
```
